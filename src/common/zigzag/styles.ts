import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  circleContainer: {
    position: 'absolute',
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  row: {
    flexDirection: 'row',
  },
  backContainer: {
    position: 'absolute',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
